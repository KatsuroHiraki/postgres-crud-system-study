# PostgreSQL CRUD System & Database Engineering

## SQL Fundamentals — DDL & DML
- Learned: Relational table structures, primary/foreign key constraints, data types, DDL (`CREATE`, `ALTER`, `DROP`), and DML (`INSERT`, `SELECT`, `UPDATE`, `DELETE`).
- Built:
  - `day_06/01_schema.sql`: DDL definitions creating `employees`, `products`, `customers`, and `orders` tables.
  - `day_06/02_seed_and_crud.sql`: Data seeding queries and basic CRUD operations executed in PostgreSQL.