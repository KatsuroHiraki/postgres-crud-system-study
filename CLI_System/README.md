## Master CRUD — Building a Python CLI Database Manager
- **Learned:** CLI loop design, exception handling for database errors (`psycopg.Error`), input parsing/validation, full CRUD operations wired into terminal user interfaces.
- **Built:**
  - `inventory_db.py`: Complete database execution layer for table mutations and queries.
  - `cli.py`: Interactive command-line inventory manager application supporting real-time terminal manipulation of PostgreSQL tables.